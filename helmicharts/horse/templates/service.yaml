apiVersion: v1
kind: Service
metadata:
  name: {{ include "horse.fullname" . }}
  labels:
    {{- include "horse.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "horse.selectorLabels" . | nindent 4 }}
